import React from 'react';
import * as IonIcon from 'react-icons/lib/io';
import './Tweet.scss';

export const Tweet = (props) => {
    return (
        <div className="tweet">

        </div>
    );
}