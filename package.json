{
  "name": "thyn",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cross-env NODE_ENV=development parcel src/index.html -p 8080 --no-cache --open",
    "build": "cross-env NODE_ENV=production parcel build src/index.html --no-minify --public-url /"
  },
  "dependencies": {
    "prop-types": "^15.6.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "cross-env": "^5.1.1",
    "node-sass": "^4.7.2",
    "typescript": "^2.6.2"
  }
}
